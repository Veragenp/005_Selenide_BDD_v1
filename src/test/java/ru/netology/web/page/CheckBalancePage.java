package ru.netology.web.page;

public class CheckBalancePage {
    //проверка баланса на карте - надо подумать
    //на странице исходный баланс, берем баланс первой карты initialBalanceFirsCard, берем баланс второй карты initialBalanceSecondCard, сохраняем в переменные
    //берем сумму перевода amountTransaction
    // в зависимости с какой карты на какую переводим делаем операцию вычитания и прибавления, может также действовать по Id, чтобы не привязываться к карте
}
